import * as ui from '/util/uiComponents.js';
import * as os from '/util/osInterface.js';

export async function createCell(parent){

    ui.createText( parent, "Cell contents are HTML elements. You can use predefined UI components as shown below to simplify cell creation.");
}